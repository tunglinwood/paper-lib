[project]
name = "paper-lib"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "crawl4ai",
    "PyMuPDF",
    "psycopg2-binary",
    "requests",
    "pikepdf",
]
